# llmfx — 開発ガイド

ダウ理論の「転換」を売買根拠とする FX 自動売買システム。
このファイルは Claude Code が作業を引き継ぐための前提資料。

## このプロジェクトで守ること

### 1. 売買判断は LLM に渡さない

エントリー・損切り・利確の判定はすべてルールベース(`llmfx/domain/`)。
LLM の役割は 3 つだけに限定する:

- **拒否権** — ルールが出したシグナルを見送るかだけを決める。新規に作らせない
- **所感の記録** — エントリー時と決済時の言語化
- **改善提案** — 蓄積した記録からパラメータ変更案を出す

理由: 判断が非決定的になるとバックテストと実運用が乖離し、検証が成立しない。
この境界を越える変更を提案するときは、必ず理由を添えて確認を取ること。

### 2. 先読み(look-ahead bias)を持ち込まない

- スイングは左右 N 本のピボットで確定するため、確定は常に N 本遅れる。
  `confirmed_index <= 現在バー` のものだけを参照する
- シグナル判定は確定足の終値のみ。既定の約定は翌足の始値
- 同一足で損切りと利確の両方に触れたら、必ず損切りが先に約定した扱い
- 逆指値・成行にはスリッページを乗せ、指値には乗せない

`tests/test_backtest.py::test_no_lookahead_truncating_data_does_not_change_past_trades`
がこれを守っている。データを途中で打ち切っても過去のトレードが 1 件も
変わらないことを検証しているので、このテストは絶対に緩めない。

### 3. 約定ロジックを二重に書かない

`llmfx/execution/fills.py` をバックテストとペーパー取引の両方が使う。
どちらか一方だけを直すと、検証した挙動が実運用で再現しなくなる。

### 4. エントリー・損切り・利確を変えるときは、必ず図で認識合わせをする

一度これを怠って、利用者の意図と実装が 3 か所ずれたまま
100 通り以上の検証を回した。ずれていたのは:

| | 利用者の意図 | こちらの実装 |
| --- | --- | --- |
| 損切り | 直近の安値 | 転換前の深い谷 |
| エントリー | 押し目からの再上昇 | 上抜けた瞬間 |
| 損切りの移動 | 新しい高値を上抜けるたび | 移動しない |

**1mm のズレはいずれ大きなズレになる。**言葉だけで合意した気になると、
検証そのものが無意味になる。仕様を変えるときは図(ローソク足の模式図)を
出して、エントリーの矢印・損切りの線・利確の線が
どこを指しているかを一つずつ確認してから実装すること。

### 5. 利確目標を RR が通るまで選び直さない

`entry.target_strategies` は「水準を出せた最初の戦略」を採用し、
その結果の RR で合否を決める。RR 2.0 を超えるまで戦略を渡り歩く実装は、
利確位置の後付けであり、フィルタが何も選別しなくなる。

## 確定済みの仕様(第 2 版・図で認識合わせ済み)

2026-08、手描きの図で突き合わせた結果、初版の仕様は利用者の意図と
3 か所ずれていた。以下が**図で確認の取れた正**。

| 項目 | 決定 | 設定キー |
| --- | --- | --- |
| 方向 | 上位足のダウ転換方向にのみ張る | `entry.higher_timeframe`, `htf_alignment_source: reversal` |
| エントリー | 上位足の転換後、下位足で**押し目を付けて再度ダウ転換した瞬間**。上抜けた瞬間ではない | `entry.mode: pullback` |
| 押し目の深さ | 条件を付けない。1 本でも下げれば押し目 | — |
| 待ち時間 | 押し目が来ないまま `pullback_max_bars` 本走ったら見送る | `entry.pullback_max_bars: 40` |
| 無効化 | 押し目が深すぎて上抜け前の水準を割ったらダマシとみなし破棄 | `entry.pullback_invalidation_atr: 3.0` |
| 損切り | **直近の安値** + ATR バッファ | `entry.stop_basis_mode: recent_swing` |
| 損切りの移動 | 新しい高値を上抜けるたびに、その手前の押し安値へ引き上げる | `execution.trail_to_structure` |
| 利確 | 固定の利確は置かない | `entry.use_take_profit: false` |
| 目標月利 | 1.4 倍。**達成必須のラインではなく、目指す方向** | `risk.monthly_target: 1.4` |

**注意: 損切りの移動だけは、合意した仕様と現在の設定が食い違っている。**
`configs/h1/*.yaml` は `trail_to_structure: false` にしてある。
図で合意したとおりに追従させると、8 銘柄 20 年で成績が単調に悪化した
(後述「決済の置き方」)。**勝手に仕様側を書き換えたのではなく、
測定結果を理由に設定側を外している。**戻すかどうかは利用者の判断。

### 初版との差(なぜ 100 通りの検証が無駄になったか)

| | 第 2 版(正) | 初版(誤) |
| --- | --- | --- |
| 損切り | 直近の安値 | 転換前の深い谷(`trend_extreme`) |
| エントリー | 押し目からの再上昇 | 上抜けた瞬間 |
| 損切りの移動 | 新しい高値ごとに引き上げ | 移動しない |
| RR フィルタ | 実質なし(`min_rr: 0.01`) | `min_rr: 2.0` |

損切り基準を直すだけで、8 銘柄 20 年の期待値が **-0.019 R → +0.086 R**
へ動いた。**このプロジェクトで単独最大の改善はパラメータ探索ではなく、
仕様のずれを直したことだった。**

`min_rr` を実質無効にしてあるのは、利確を置かない構成では
エントリー時点で「リワード」が定義できないため。RR で選別したければ
利確を戻すことになるが、それは勝ちの頭を押さえる(後述)。

### 利確目標が trend_origin である理由

損切りを転換前の極値に置くと、リスク幅は必ず「直前の波の全長」になる。
そのため利確目標が波 2 本分より遠くないと RR 2.0 に届かない。実測では:

| 利確目標 | RR 中央値 | RR >= 2.0 の割合 |
| --- | ---: | ---: |
| `structure`(最も近い抵抗) | 0.55 | 1.2% |
| `measured_move`(波 1 本分) | 0.85 | 約 2% |
| `trend_origin`(トレンド起点) | 1.98 | 49.4% |

## 目標月利の扱い

月利 1.4 倍は非常に高い。**数字を良く見せない**こと。

`backtest` のレポート末尾と `llmfx target` は、目標達成に必要なリスク率を
対数成長の式から逆算し、モンテカルロで破産確率と最大ドローダウンを出す。
「到達不能」と出たらそう表示する。楽観的な見通しに書き換えない。

## コマンド

```bash
pip install -r requirements.txt
python -m pytest -q                     # テスト(264 件)

# GMOコインから暗号資産のローソク足を取得(口座・APIキー不要)
python -m llmfx.cli data fetch-gmo --symbol BTC_JPY --granularity M15 \
    --start 2021-04-15 --end 2026-08-19 --out data/btcjpy_m15.csv
python -m llmfx.cli backtest --config configs/btcjpy_gmo.yaml --data data/btcjpy_m15.csv

# 楽天 MT4 などからエクスポートした CSV を取り込む
python -m llmfx.cli data import-mt4 --in <MT4のCSV> --out data/usdjpy_m15.csv --server-tz-offset 2

# HistData から FX の足をまとめて作る(口座不要、2000 年以降)
# 検証に使う 30 銘柄はこれで作る。1 銘柄あたり 4〜5 分
python -m llmfx.cli data fetch-histdata --symbols USDJPY EURUSD GBPJPY \
    --granularity H1 --out-dir data --skip-existing

python -m llmfx.cli backtest --config configs/default.yaml --data data/usdjpy_m15.csv --journal data/journal.sqlite
python -m llmfx.cli diagnose --config configs/default.yaml --data data/usdjpy_m15.csv
python -m llmfx.cli paper    --config configs/default.yaml --replay data/usdjpy_m15.csv
python -m llmfx.cli review   --journal data/journal.sqlite --llm --out out/review.md
python -m llmfx.cli target   --win-rate 0.35 --win-r 2.3 --loss-r 0.7 --trades-per-month 17
```

`ANTHROPIC_API_KEY` が無くても全機能が動く(LLM 層が自動的に無効化される)。

## 利用者の運用環境

- 普段の取引は **楽天FX**。データは楽天 MT4 のヒストリーセンターから CSV エクスポート
- **楽天FX には Python から直接発注できる公開 API が無い**。自動売買の正規ルートは MT4 の EA(MQL4)
- 実弾へ繋ぐときは、判断を Python 側に残したまま MT4 には発注だけさせるブリッジ構成にする。
  戦略ごと MQL4 へ移植すると LLM 層が使えなくなる

## 実データでの検証結果(2026-08、BTC_JPY M15)

GMOコインの Public API から 185,645 本(2021-04-15 〜 2026-08-18、5.3 年、
価格 215 万〜1,900 万円)を取得して検証した。**この設定にエッジは無い。**
数字を良く見せないという方針どおり、そのまま残す。

### 生のシグナルに予測力が無い

| 条件 | 取引 | 勝率 | 必要勝率 | 差 | 期待値 | PF |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 既定(コスト・決済管理あり) | 3,026 | 26.1% | 30.7% | -4.5% | -0.100 R | 0.72 |
| コストのみ除去 | 3,026 | 27.8% | 29.6% | -1.8% | -0.038 R | 0.82 |
| 決済管理のみ除去 | 1,737 | 20.0% | 21.8% | -1.8% | -0.085 R | 0.84 |
| **両方除去(生のシグナル)** | 1,737 | **20.2%** | **20.5%** | **-0.3%** | **-0.016 R** | **0.90** |

コストも決済管理も外した「生のダウ転換」でほぼコイン投げ。
**コストのせいで負けているのではない。**元々エッジが無い。

損切り基準 x 利確目標の 8 通り、時間足 4 種(M15/H1/H4/D1)もすべてマイナス。
時間足は上げるほど悪化する(M15 -0.085 → H4 -0.227)。

### 勝率と必要勝率は連動する(最重要)

利確目標を近づければ勝率は上がる。だが損益分岐に必要な勝率も同じだけ上がる。

| min_rr | 取引 | 勝率 | 必要勝率 | 差 | 平均勝 | 期待値 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 0.5 | 836 | 30.5% | 32.7% | -2.2% | 2.14 R | -0.069 R |
| 1.0 | 740 | 27.0% | 28.9% | -1.9% | 2.56 R | -0.068 R |
| 2.0 | 522 | 21.1% | 22.6% | -1.6% | 3.57 R | -0.072 R |
| 3.0 | 344 | 18.9% | 19.2% | -0.3% | 4.41 R | -0.018 R |
| 4.0 | 223 | 18.4% | 17.0% | +1.4% | 5.16 R | +0.088 R |

(上位足 H4 方向一致・極値 2.0 ATR 以内)

**「1 回の勝ちを小さくして負ける回数を減らす」は、この軸を動かしても達成できない。**
勝率と RR は独立した 2 つのつまみではなく、同じ分布の 2 つの見方だから。
差(勝率 − 必要勝率)を動かせるのは、シグナルの予測力そのものだけ。

### 上位足フィルタも救済にならない

「上位足のダウ転換方向と一致し、かつ上位足の転換後に付けた極値(押し安値 /
戻り高値)の付近でのみ、下位足のダウ転換でエントリー」を実装して検証した。

| 条件 | 取引 | 勝率 | 必要勝率 | 期待値 |
| --- | ---: | ---: | ---: | ---: |
| フィルタなし | 1,737 | 20.0% | 21.8% | -0.085 R |
| H1 方向一致のみ | 647 | 21.9% | 25.4% | -0.142 R |
| H4 方向一致のみ | 1,072 | 20.8% | 23.3% | -0.112 R |
| D1 方向一致のみ | 1,199 | 20.4% | 22.5% | -0.101 R |
| H4 + 極値 0.5 ATR | 72 | 25.0% | 21.0% | **+0.202 R** |
| H4 + 極値 1.0 ATR | 327 | 19.6% | 22.1% | -0.120 R |
| H4 + 極値 2.0 ATR | 522 | 21.1% | 22.6% | -0.072 R |

**方向一致だけでは悪化する。**距離を足すとプラスのセルが出るが、偶然の範囲。
プラスに見えた候補 3 つは、すべて同じ形で崩れた。

| 候補 | 取引 | 期待値 | 95% 信頼区間 | 期間 3 分割 |
| --- | ---: | ---: | --- | --- |
| H4 + 極値0.5ATR + RR≥0.5 | 102 | +0.072 R | -0.310 〜 +0.481 | +0.519 / -0.216 / -0.074 |
| H4 + 極値2.0ATR + RR≥4.0 | 223 | +0.088 R | -0.234 〜 +0.425 | -0.106 / +0.041 / +0.316 |
| D1 + 極値2.0ATR + RR≥1.5 | 598 | -0.004 R | -0.161 〜 +0.161 | +0.213 / -0.036 / -0.165 |

**黒字がすべて特定の 1 期間だけから来ている。** 信頼区間もすべて 0 をまたぐ。
距離の方向で見ても単調でない(H4: 1.5→-0.104, 1.0→-0.118, 0.75→-0.152,
0.5→+0.072)。上位足を D1 にすると符号が逆になる。機構として成立していない。

ただし**最大ドローダウンは大きく改善する**(97.1% → 15.8〜73.0%)。
これは選別が効いたのではなく、負ける機会に晒される回数が減っただけ。

### 大標本での決着(2026-08、FX 8 銘柄 x 20 年)

HistData から 8 銘柄・最大 26 年(合計 492 万本)を取得し、開発用
(2000〜2019、20 年)で検証した。取引 22,264 件。**ここで決着がついた。**

### ダウ転換のブレイク買いは、統計的に明確な負け

| 条件 | 取引 | 期待値 | t 値 | プラスの銘柄 |
| --- | ---: | ---: | ---: | ---: |
| フィルタなし | 22,264 | -0.114 R | **-7.68** | 0/8 |
| H1 方向一致 | 9,530 | -0.084 R | -4.44 | 1/8 |
| H4 方向一致(転換) | 16,848 | -0.106 R | -6.92 | 0/8 |
| H4 方向一致(トレンド) | 14,321 | -0.090 R | -5.08 | 0/8 |
| D1 方向一致 | 19,167 | -0.132 R | -8.89 | 0/8 |
| H4 + 極値 1.0ATR | 5,733 | -0.107 R | -3.88 | 0/8 |
| min_rr 0.5 〜 4.0 | 17k〜26k | -0.100 〜 -0.142 R | -7.4 〜 -8.2 | 0/8 |
| ATR 分位フィルタ | 22k | -0.108 〜 -0.112 R | -7.2 〜 -7.6 | 0/8 |
| 9-14 時のみ | 8,075 | **+0.037 R** | +1.63 | 6/8 |

**上位足フィルタはここで完全に否定された。**全変種が有意にマイナスで、
8 銘柄中 0〜1 しかプラスにならない。小標本(3 ペア 3.7 年)で見えた
+0.276 R は錯覚だった。

同様に、**「ロンドン 7-16 が良い」も否定された。**大標本では 7 時 t=-2.09、
8 時 t=-2.26 で有意にマイナス。唯一プラス側なのは 9-14 時だが t=+1.63 で有意でない。

### 負けはコストのせいではない(いちばん重要)

| | 期待値 | t 値 |
| --- | ---: | ---: |
| コストあり | -0.114 R | -7.68 |
| **コストなし** | **-0.071 R** | **-5.33** |
| 差(= コスト) | -0.043 R | |

スプレッドもスリッページもゼロにして、なお **-0.071 R (t=-5.33)**。
**値動きそのものがブレイク方向と逆に偏っている。**ダウ転換の上抜けを
買うと、その後は下がりやすい。いわゆるダマシが統計的に確認された。

これは 22,264 件・8 銘柄・20 年での測定であり、偶然では説明できない。

### ここから導かれること

負けが安定して有意なら、**逆に張れば勝てる可能性がある。**
往復コストはどちら向きでも掛かるが、素の偏りが -0.071 R あるので、
コスト -0.043 R を差し引いてもまだ余地が残る計算になる。

ただし単純な符号反転ではない。損切りと利確が入れ替わると R の意味が
変わるため、ダマシを取る設計を別に作る必要がある(下記「次にやること」)。

### 注意

- 時間帯フィルタで取引数が **増える**ことがある(18-24 時除外で 22,264 → 26,938)。
  同時保有 1 建玉なので、あるシグナルを見送ると別のシグナルを拾える枠が空くため。
  バグではない
- 非 JPY 銘柄の `quote_to_account_rate` は 1.0 のまま。円換算の絶対額はずれるが、
  **R 倍数は換算レートが分子分母で相殺するため正しい**。分析はすべて R で行う

## 逆張り(fade)の検証結果 — エッジは実在するが、コストに届かない

順張りが有意に負ける以上、逆に張れば取れるはず、という仮説を検証した。
開発用(2000-2019、8 銘柄)。

### 偏りは実在する

コストをゼロにした「素の値動き」で、逆張りは有意にプラスだった。

| 損切り幅 | 取引 | コストなし | t 値 | コスト | 差引 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 0.6 ATR | 68,885 | +0.026 R | +5.25 | -0.077 | -0.051 |
| 1.0 ATR | 85,919 | +0.033 R | +7.41 | -0.054 | -0.021 |
| 1.5 ATR | 80,447 | +0.023 R | +5.25 | -0.037 | -0.014 |
| 2.5 ATR | 57,822 | +0.025 R | +4.82 | -0.024 | +0.001 |
| 3.5 ATR | 44,766 | +0.028 R | +5.74 | -0.017 | +0.011 |

**ダウ転換のブレイク後に押し戻される傾向は本物。**8 万件超で t=+7.41。
偶然では説明できない。

### だが、コストのほうが大きい

素のエッジは +0.025 〜 +0.033 R で頭打ちになる。一方コストは損切り幅で
決まり、損切りを広げるほど R が大きくなってコストの比率が下がる。
両者が交差するのが 2.5 ATR あたり。そこを超えても:

    損切り 3.5 ATR / 利確 1.0R / 41,521 件
    期待値 +0.004 R  t=+0.71  95%CI -0.008 〜 +0.015  4/8 銘柄がプラス

**±0 止まり。**信頼区間は 0 をまたぐ。取引できるエッジではない。

### この効果は M15 にしか無い

| 足 | 逆張り(コストなし) | t 値 |
| --- | ---: | ---: |
| M15 | **+0.033 R** | **+7.41** |
| H1 | -0.003 R | -0.51 |
| H4 | -0.053 R | -4.31 |

短い時間軸の現象で、H1 で消え、H4 では逆になる。
「上位足なら ATR が大きくコストの比率が下がる」という当ては外れた。
エッジ自体が上位足に存在しないため。

### 結論

**ダウ理論のブレイクには、順張りにも逆張りにも、小売のコストを超える
エッジが無い。**逆張りの素のエッジ(+0.03 R)は実在するが、小売のスプレッド
(0.02〜0.08 R)がそれ以上を食う。損切り幅を最適化しても ±0 が上限。

残る手は執行コストを下げることだけ:
- **指値でのエントリー**。入り側のスプレッドを払わずに済み、コストは概ね半減。
  ただし指値は約定しないことがあり、**不利な時だけ約定して有利な時は
  置いていかれる**(逆選択)。この偏りはバックテストでは楽観的に出るため、
  節約分がそのまま残るとは考えないこと
- 最も狭い銘柄だけを選ぶ(USD/JPY 0.2 pips 対 GBP/JPY 0.9 pips)

## 決済の置き方(開発用 2000-2019・8 銘柄)

利確とトレーリングをどう置くかで、成績が大きく変わる。

| 条件 | 取引 | 勝率 | 平均勝 | 最大勝 | 期待値 | t |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 利確あり・追従なし | 22,264 | 18.2% | 4.04 R | 31.3 | -0.114 | -7.68 |
| 利確なし・M15 で追従 | 25,890 | 26.9% | 1.72 R | 30.1 | -0.115 | -10.82 |
| 利確なし・H1 で追従 | 29,661 | 21.1% | 3.13 R | 54.6 | -0.054 | -3.67 |
| 利確なし・H4 で追従 | 25,143 | 17.9% | 4.15 R | 88.0 | -0.045 | -2.20 |
| 利確なし・D1 で追従 | 21,842 | 13.8% | 5.97 R | 84.9 | -0.051 | -2.21 |
| **利確なし・追従なし** | 22,431 | **11.7%** | **7.58 R** | **84.9** | **-0.028** | **-1.16** |

### 分かったこと

**固定の利確は勝ちの頭を押さえている。**外すと平均勝ちが 4.04 R から
7.58 R へ、最大勝ちが 31 R から 85 R へ伸びる。期待値も -0.114 から
-0.028 へ改善し、t が -7.68 から -1.16 になる。**統計的に「負けている」と
言えない領域まで来た唯一の構成。**

**トレーリングは掛けるほど損をする。**基準足を上げる(= 追従を遅くする)ほど
成績が良くなり、その極限が「追従なし」。方向性のエッジが無い信号に
トレーリングを掛けると、**太い右の裾を細かい勝ちに変換するだけで、
負けはそのまま残る。**勝率は 11.7% → 26.9% と上がるが、利幅の合計は減る。

「押し安値が確定するのは値動きが一度戻った後」なので、伸びる途中の
調整で必ず引っかかる。「調整波で落とされる」現象そのものがトレーリングで
起きている。

### 保有期間

利確なし・追従なしの構成では、時間切れが唯一の出口になる。掃引すると
400 本(M15 で約 4.2 日)が最良で、1,000 本まで伸ばすと -0.104 に悪化した。

## 検証の作法(次に同じ轍を踏まないため)

約 70 通りを試している。この規模で探せば、偶然プラスになるセルは必ず出る。
プラスが出たら必ず次を通すこと:

1. **近傍でも成立するか** — 1 点だけ突出しているならスパイク(偶然)
2. **期間を分割しても成立するか** — 符号が反転したら偶然
3. **ブートストラップの信頼区間が 0 をまたがないか**
4. **別の銘柄でも成立するか** — 機構が本物なら通貨ペアでも出るはず

### 先読みが無いことは実データでも確認済み

データを 50% / 70% / 90% で打ち切っても、それ以前に決済が完了したトレード
(1,514 / 2,140 / 2,739 件)が 1 件も変わらない。この負けは実装バグではない。

### 取引コストの実額(GMOコイン)

| 前提 | 取引 | 期待値 | 期間コスト |
| --- | ---: | ---: | ---: |
| コストゼロ(理論値) | 3,026 | -0.038 R | 0 円 |
| 暗号資産FX(実勢) | 3,026 | -0.100 R | 39,826 円 |
| 取引所現物・買いのみ | 1,459 | -0.243 R | 383,801 円 |

暗号資産FX は取引手数料 0、コストはスプレッド実測 0.9〜1.4 bp と
レバレッジ手数料 0.04%/日。**現物は Taker 0.05% の往復でコストが約 10 倍**
になるうえ売り建てができずシグナルが半減する。やるならレバレッジ側。

## 過去に踏んだ罠(繰り返さないこと)

- **`position_size` の整数丸め** — `math.floor(units)` は FX の通貨単位前提。
  BTC は 1 単位が数百万円なので、正しい建玉サイズが全部 0 になっていた。
  `instrument.size_step` / `min_order_size` を必ず銘柄に合わせる
- **レバレッジ上限の未設定** — 損切りが近いと建玉が資産の 14 倍に膨らむ。
  国内の暗号資産は法令で 2 倍が上限。`risk.max_leverage` を設定する
- **固定 pips のコスト** — BTC は 5 年で価格が 8.8 倍動く。`execution.spread_bps` を使う
- **GMO API の 404** — 未来日付は JSON エラーではなく HTTP 404。期間の末尾は
  JST 6:00 区切りの都合で 1 日先まで問い合わせるため、例外にすると必ず落ちる
- **上位足の距離を下位足の ATR で測る** — 物差しが小さすぎてほぼ全部が
  「遠い」判定になる。`htf_proximity_atr` は上位足の ATR を基準にしている

## 実データでの検証結果

### 暗号資産(BTC_JPY M15、GMOコイン、2021-04 〜 2026-08、185,645 本)

**エッジ無し。**コストも決済管理も外した「生のダウ転換」で勝率 20.2%、
損益分岐に必要な勝率 20.5%、期待値 -0.016 R。ほぼコイン投げでわずかに負け側。
損切り基準 x 利確目標の 8 通り、時間足 4 種、すべてマイナス。
**コストのせいで負けているのではない。**元々エッジが無い。

### FX 3 ペア(USD/JPY・GBP/JPY・EUR/JPY、HistData、同期間、各 約 129,600 本)

開発用(2021-04 〜 2024-12)での期待値 R。決済管理なし・コスト実勢。

| 条件 | USD/JPY | GBP/JPY | EUR/JPY | 符号 |
| --- | ---: | ---: | ---: | --- |
| フィルタなし | +0.027 | -0.055 | -0.143 | 割れる |
| H4 方向一致(転換) | -0.046 | +0.080 | -0.048 | 割れる |
| H4 方向一致(トレンド) | -0.006 | +0.082 | -0.034 | 割れる |
| H4 + 極値 1.0 ATR | -0.040 | -0.104 | -0.052 | 全部マイナス |
| H1 方向一致 | +0.030 | -0.167 | -0.128 | 割れる |
| D1 方向一致 | +0.032 | -0.154 | -0.076 | 割れる |

**上位足フィルタは 3 ペアでの追試に耐えなかった。**一貫しているのは
「極値付近に絞ると 3 ペアとも悪化する」ことだけ。

以前 USD/JPY 単独で +0.218 R が出たが、あれは (1) 全期間(検証用を含む)を
見ていた、(2) Dukascopy のデータ(週末バーを含み、本数が HistData より 44%
多い)を使っていた、の 2 点で汚れていた。再現しない。

### 時間帯 — 唯一まともに揃った軸、ただし検証用では確認できず

開発用での期待値 R。3 ペアすべてで符号が揃った。

| 時間帯(UTC) | USD/JPY | GBP/JPY | EUR/JPY | 平均 |
| --- | ---: | ---: | ---: | ---: |
| ロンドン 7-16 | +0.195 | +0.095 | +0.067 | **+0.119** |
| ロンドン序盤 7-11 | +0.092 | +0.301 | +0.169 | **+0.187** |
| 東京深夜 21-24 | -0.143 | -0.378 | -0.291 | **-0.271** |

流動性が入る時間帯でブレイクが機能し、薄い時間帯で機能しない、という
機構の説明がつく。ここへ H4 方向一致を重ねると開発用で:

    3 ペア合算 395 件  +0.276 R  t=+2.34  95%CI +0.053 〜 +0.518

このプロジェクトで初めて信頼区間が 0 を外れた。そこで検証用へ 1 回かけた。

    検証用 225 件  +0.051 R  t=+0.41  95%CI -0.185 〜 +0.293
    内訳: USD/JPY -0.184 / GBP/JPY -0.110 / EUR/JPY +0.437

**確認できなかった。**符号は保たれたが有意でなく、3 ペア中 2 ペアがマイナス。
+0.276 → +0.051 への縮み方は、選択バイアスに掛かった結果の典型的な形。

**この仮説に対する検証用データは使い切った。**ここから条件を弄って
再挑戦することはできない。やるなら別の期間か別の銘柄を用意する。

### 統計の扱いで注意すること

- **JPY クロス 3 ペアは独立ではない。**すべて円を共有するため同じ方向へ動く。
  3 ペア合算の信頼区間は、独立を仮定している分だけ楽観的に出る
- 時刻ごとに分解すると隣り合う時間で符号が反転する(17 時 +0.684 / 18 時 -0.437)。
  細かい時刻の凹凸はノイズ。帯として見たときだけ意味がある

## 負け側も同じ厳しさで潰す(利用者の指摘、2026-08-23)

> 勝ち点に対してバグがないかを精査して潰すのは素晴らしいが、これは
> 負け側にもしないといけないのでは。今は、勝ちに対して厳しく精査して
> いるのに、負けはそのまま受け入れているので、構造的に負けやすくなって
> いる。

**そのとおりだった。**誤りは勝ち側にも負け側にも等確率で入る。片側だけ
潰せば、**測定そのものが「エッジ無し」へ倒れる。**

### 決め手は鏡像との突き合わせ

同じ合図・同じ足・同じ値段で **向きだけ裏返す**(`reverse_entry`)。
値動きは片方が損なら片方は得なので:

| 素の期待値 | 意味 |
| --- | --- |
| 順 + 逆 ≒ 0 | **仕掛けは値を削っていない。**負けは方向の話 |
| 順 + 逆 が明らかに負 | **仕掛けそのものが値を削っている = バグ** |

第4版でこれを回して、**帯の指値は和がほぼ 0(方向の話)、ドテンは
両向きともマイナス(仕掛けの問題)**と分かれた。合算だけ見ていたら
区別できない。

### この日に負け側で見つけた誤り

| 誤り | 効き | 中身 |
| --- | ---: | --- |
| 決済の順序を一律に損切りへ寄せていた | +0.025 R | 損切りと転換ラインは売りなら **両方が上**。価格は下から来るので近いほうが先に着く。曖昧なのは反対側どうしの水準だけ |
| **その足が付けていない値段で約定していた** | **+0.122 R** | 支持帯の指値が価格のはるか上に残り、`安値 <= 指値` だけで約定。123.4 の足が 131.2 で約定して -16 R を計上していた |
| 待ち伏せの解除で約定判定ごと飛ばしていた | — | 注文は離れているあいだも置いてある。「近いときだけ見る」にすると、再武装の距離を広げるほど選別が効いたように見える |

**帯の指値は -0.217 から -0.070 になった。元の負けの 3 分の 2 は
バグだった。**

### 勝ち側も同じ回で 1 つ見つかっている

約定した足で損切りを一度も見ていなかった。建玉を翌足からしか評価して
いなかったため、**損切りが狭いほど「同じ足で切られたはずの負け」を
見逃す**。他所と同じ推し量り方(陽線 始値→安値→高値→終値)で、
**約定より後だけ**を見るよう直した。

### 手順

プラスが出たときと **同じ手順を、マイナスにも掛ける**:

1. 鏡像の和はゼロか(仕掛けが値を削っていないか)
2. コストゼロでも同じ符号か
3. 極端な値のセルは物理的に置けるか(pips に直す)
4. 単調な軸の端は、機構ではなく **見落とし** が作っていないか

## 約定した足の道順が、結論をまるごと決めることがある(2026-08-23)

**水準に置いた注文で約定する形は、その足の中の順序に乗る。**損切りが
足 1 本の値幅の内側に入るため。挟んで確かめると:

| 損切り | 陽線 始値→安値→高値→終値 で解く | 不利側の端を必ず数える | 幅 |
| --- | ---: | ---: | ---: |
| 0.3 ATR | **+0.781** | **-0.802** | 1.584 |
| 1.0 ATR | +0.085 | -0.425 | 0.510 |
| 1.5 ATR | +0.063 | -0.199 | 0.262 |
| 2.5 ATR | +0.038 | -0.022 | 0.061 |

**どの幅でも符号をまたぐ。**掃引で「損切りを詰めるほど良い」と単調に
出ていたのは、詰めるほどこの仮定に乗る量が増えるから。機構ではない。

### 逃げ道は 2 つしかない

1. **M1 で道順を解く。**`intrabar="path"` と同じ扱い。データが要る
2. **水準では約定させない。**合図を見てから **次の足の始値** で入る。
   その足は丸ごと約定より後なので、順序を問う余地が消える

第3版(弾いた足を見てから次の足の始値で乗る)が 3 通りの仮定で
一致したのは、2 を満たしていたため。**同じ帯・同じ損切りでも、
入り方をこちらにするだけで結論が決まるようになる。**

**損切りを広げるほど幅は縮む**(2.5 ATR で 0.061)。決めきれない
ときは、幅が十分小さい領域だけで語ること。

## 第5版(三段)の測定 — ドテンと買い増しが初めてプラスになった

帯 H4 / 構造 H1 / 執行 M15。ドテンと買い増しは、構造の水準まで待たず
**執行の足が自分の折り返しを出したところ**で入る。帯の指値だけは従来
どおり(利用者の指定)。条件探しに使う 4 銘柄・開発用 2000-2019。

| 向き | 約定の仕方 | 期待値 R | t | R/月 | プラス |
| --- | --- | ---: | ---: | ---: | ---: |
| 順側(帯で跳ね返りを取る) | 水準 | -0.107 | -11.7 | -9.1 | 0/4 |
| **逆側(帯を抜けた方向)** | **水準** | **+0.082** | **+4.19** | **+6.6** | 3/4 |
| 逆側 | 次の足の始値 | +0.052 | +2.92 | +4.3 | 3/4 |

### 機構ごと(逆側・水準で約定)

| 機構 | 件数 | 勝率 | 平均勝 | 期待値 R | t | プラス |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| zone(帯の指値) | 9,774 | 23.9% | +3.45 | **+0.108** | +3.87 | 3/4 |
| **flip(ドテン)** | 1,952 | 25.5% | +1.52 | **+0.048** | +0.73 | 3/4 |
| **add(買い増し)** | 6,234 | 29.1% | +1.98 | **+0.051** | +1.77 | 2/4 |

**三つとも同時にプラスになったのは初めて。**ドテンの損切りを「1 つ前」へ
直したことと、入り口を執行の足へ下ろしたことの両方が効いている。

### 掃引した 2 軸の答え

| 軸 | 結果 |
| --- | --- |
| **B 執行の足が折り返さなかったら** | **どちらでも変わらない**(+0.082 / +0.081)。構造の水準で乗っても、見送っても同じ |
| **A 水準で約定させるか、次の足の始値か** | 合算で +0.082 / +0.052。**差は flip と add にだけ出る**(zone は +0.108 / +0.109 で不動) |

**ドテンの黒字は、決まらない側にしか無い。**水準で約定させると +0.048、
次の足の始値にすると **-0.098**。約定した足の順序をどう仮定するかで
符号が変わる。買い増しも +0.051 → +0.009 と消える。

### 帯の指値は、まだ挟みが閉じない

| 道順 | zone | 合算 |
| --- | ---: | ---: |
| 陽線 始値→安値→高値→終値 で解く | **+0.108** | +0.082 |
| 不利側の端を必ず数える | **-0.109** | -0.060 |
| 幅 | 0.218 | 0.142 |

**ただし「不利側の端を必ず数える」は、約定より前の値動きを数えている
ので、行き過ぎた床。**抜けた方向へ買うと、その足の安値はたいてい
上げ始める前 = 約定より前に付く。真値は 2 つの間で、推し量った側に
近いはず。**決着させるには M1 が要る。**

## 検証の作法(必ず通すこと)

**経過した「期間」を数字で書かないこと。**一度うっかり「5 か月」と書いたら、
自分でそれを読み返して何度も繰り返し、利用者に訂正された(実際は 2 週間)。
資料には**試した通り数・銘柄数・件数**のような、確かめられるものだけ書く。

これまでに 100 通り以上を試している。この規模で探せば偶然プラスになる
セルは必ず出る。プラスが出たら:

1. **近傍でも成立するか** — 1 点だけ突出しているならスパイク
2. **期間を分割しても成立するか** — 符号が反転したら偶然
3. **ブートストラップの信頼区間が 0 をまたがないか**
4. **別の銘柄でも成立するか** — **条件探索に使っていない銘柄で試すこと。**
   探索に使い回した銘柄で確かめても、独立した検証にならない。
   実際、使い回した 16 銘柄で +0.257 R(t=+1.74)だったものが、
   新しく足した 12 銘柄では -0.406 R(t=-2.85)だった
5. **検証用データで 1 回だけ確かめる** — `--split holdout`。既定は dev。
   ここで数字を見た後に条件を弄ったら、その期間はもう検証用ではない

## 過去に踏んだ罠(繰り返さないこと)

- **`position_size` の整数丸め** — `math.floor(units)` は FX の通貨単位前提。
  BTC は 1 単位が数百万円なので建玉が全部 0 になっていた
- **レバレッジ上限の未設定** — 損切りが近いと建玉が資産の 14 倍に膨らむ
- **固定 pips のコスト** — BTC は 5 年で価格が 8.8 倍動く。`spread_bps` を使う
- **上位足の距離を下位足の ATR で測る** — 物差しが小さすぎて標本が消える
- **`DowAnalyzer.trend` を「いまのトレンド」として使う** — 転換時にラッチ
  されるので `bias` と同じ値になる。`structure_trend()` を使う
- **絶対水準の決め打ち** — 「USD/JPY 164 円は介入が来るから避ける」は、
  164 が壁だと分かったのが 2024 年の介入後である以上、先読み
- **データ源による差** — 同じ USD/JPY でも Dukascopy は HistData より
  本数が 44% 多い(週末バーを含む)。データ源を変えたら結果も変わる
- **1 本の足の中の道順を仮定する** — 四本値では高安の順序が分からない。
  最値に指値を置く手法では、この仮定だけで期待値が +0.126 から -0.184 まで
  動いた。**M1 を渡して解くこと**(`intrabar="path"`)
- **決済先の帯を「平均値が向こう側にある帯」で選ぶ** — 決済は手前の縁で
  行うので、幅の広い帯だと縁が指値のこちら側へ回り込み、**損切りを飛び越えた
  地点で「利確」**する。実データで負けの半分がこれ(平均 -3.78 R、最悪 -35 R)
- **約定足の順行を高安そのままで取る** — 指値は不利側の端で約定するので、
  その足の有利側の端は約定より前に付いていることが多い。それを順行に
  数えると **持ってもいない建玉の含み益**を数える。建値へ損切りを動かす
  判定に使うと、実測で 97% が約定足だけで発火し(約定より後だけなら
  31%)、10 銘柄の期待値が **-0.038 から +0.036 へ反転**した。丸ごと作り物
- **`SwingDetector.swings` を差分だけで読む** — 同じ向きが続くと末尾を
  「より極端な方」で **置き換える**ので列が伸びない。差分(`swings[seen:]`)
  だけ見ていると置き換えを取りこぼす
- **コンテナが巻き戻る** — `data/` は git に入っていないので、作り直された
  ら取り直しになる。コードは必ず push しておくこと
- **`requirements.txt` に実在しない版を書く** — `histdata>=1.4` は PyPI に
  無く(あるのは 1.0 と 1.1)、`pip install -r requirements.txt` がそこで
  丸ごと失敗する。環境が作り直されるまで気づかなかった
- **データ取得を使い捨てのスクリプトに置く** — 環境を作り直すたびに
  データの作り方ごと消える。`llmfx data fetch-histdata` に移した
- **1 銘柄の失敗で全体を止める** — 404 の HTML が .zip として届くことがある。
  30 銘柄を回している最中に 1 つ壊れると、そのプロセスが担当する残りが
  全部落ちる。1 年分を諦めるだけにすること
- **壊れた足を掃除しないまま検証する** — HistData の生データには桁の
  飛んだ足が混ざっている。最初は `close` だけを見て弾いていたが足りず、
  **四本値すべてと値幅(1 本で 25% 以上動く足)** まで広げて初めて消えた。
  掃除前に出ていた H4/M30 の **+0.881 R は、掃除後 -0.019 R** になった。
  異常に良い数字が出たら、まずデータを疑うこと(`llmfx/data/sanitize.py`)

## データの取得

| 銘柄 | 取得元 | 所要 | 備考 |
| --- | --- | --- | --- |
| BTC_JPY | GMOコイン Public API | 数分 | 口座不要。2021-04-15 以降 |
| FX 各種 | HistData | 1 年 3 秒 | **2000 年以降**。1 年 1 ファイル |

Dukascopy も実装してあるが、1 日 1 ファイルのため 5 年分で約 1,900
リクエストになり、レート制限で使えなくなった(30 中 28 が 503)。
FX は HistData を使うこと。

## 押し目待ちエントリー(第 2 版仕様)の検証結果

図で認識合わせをした仕様(`entry.mode: pullback`)を実装して検証した。

### 損切り基準を直した効果が、単独で最大だった

`stop_basis_mode` を `trend_extreme`(誤)から `recent_swing`(正)へ
直しただけで、FX 8 銘柄 x 20 年の期待値が動いた:

    trend_extreme  -0.019 R
    recent_swing   +0.086 R

**探索で稼いだ改善ではなく、仕様のずれを直して出た改善。**
損切りを転換前の極値に置くとリスク幅が「直前の波の全長」になるため、
1 回の負けが構造的に大きくなりすぎていた。

### 時間足の組み合わせ(2 つずらし)

利用者の指定は「10 分なら 1 分、15 分なら 5 分のように 2 つずらし」。
M1 は不安定で再現性が無いという判断で除外し、以下を検証した。

    M5-M15 / M10-M30 / M15-H1 / M30-H4 / H1-D1

**H1(下位足)x D1(上位足)が最も安定した。**下位足を細かくするほど
標本は増えるが、押し目の判定がノイズに埋もれて成績が落ちる。

### 現時点の D1 → H1(FX 4 銘柄・開発用 2000-2019)

| 建値移動 | 取引 | 勝率 | 平均勝 | 期待値 | t | 銘柄の符号 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| なし | 183 | 19.1% | 4.31 R | +0.016 | +0.09 | +-++ |
| 3.0R で建値へ | 183 | 17.5% | 4.62 R | **+0.076** | +0.43 | +-++ |
| 2.0R | 183 | 14.8% | 4.85 R | +0.065 | +0.38 | +-++ |
| 1.5R | 183 | 12.6% | 5.16 R | +0.058 | +0.36 | --++ |
| 1.0R | 183 | 10.9% | 5.23 R | +0.067 | +0.44 | --++ |
| 0.5R | 183 | 6.6% | 5.38 R | -0.009 | -0.07 | ---+ |

**符号は初めて安定してプラス側だが、183 件では何も言えない。**
t が +0.5 にも届いていない。作法 3(信頼区間が 0 をまたがないか)を
まったく通っていないので、**この数字を根拠に何かを決めてはいけない。**

勝率 19.1% x 平均勝ち 4.31 R は、損益分岐(必要勝率 18.8%)のほぼ真上。
「わずかにプラス側のコイン投げ」から動いていない。

### 次に必要なのは条件いじりではなく件数

エントリー条件を厳しくしたので、1 銘柄あたり 45 件しか出ない。
**同じ条件のまま銘柄を 18 へ増やして件数を稼ぐ**のが先。
`configs/h1/*.yaml` に 18 銘柄分を用意してある(JPY クロス、ドルストレート、
クロス通貨、金、株価指数)。相関の低い銘柄を混ぜてあるのは、
JPY クロスだけで揃えると独立でない標本を数えることになるため。

## 建玉はどこまで動いたか(MAE / MFE)

「なぜ負けたか」を集計値から読むための道具。`llmfx/backtest/inspect.py`。

最大逆行(MAE)= 損切り方向へ最も進んだ幅、最大順行(MFE)= 利益方向へ
最も進んだ幅。どちらも R 倍数に直してから比べる(リスク幅が銘柄ごとに
違うため、価格のままでは比較できない)。

D1 → H1 の押し目エントリー・FX 4 銘柄・開発用 183 件での実測:

| | 件数 | 逆行の中央値 | 順行の中央値 | 1.0R 到達 |
| --- | ---: | ---: | ---: | ---: |
| 勝ち | 35 | 0.66 R | 5.90 R | 100% |
| 負け | 148 | 0.82 R | 0.73 R | **38.5%** |

**負けの 38.5% は、一度 1.0R まで勝ちに向かってから戻された。**
一度も動かないまま切られたのは 22.3% だけ。つまり負けの大半は
「入る場所が悪かった」のではなく「持ち方で失った」もの。

### 負けを R の大小で分けても何も見えない

損切りがきっちり効くので、負けは全部ちょうど -1.0R に揃う。
分布を見るなら **順行したかどうか**で割ること。打つ手が真逆になる:

| 分類 | 意味 | 効く手 |
| --- | --- | --- |
| 順行してから戻された(MFE >= 1.5R) | 決済の置き方の問題 | 建値移動、部分利確 |
| 一度も順行しなかった(MFE < 0.2R) | エントリーの質の問題 | 入る条件の選別 |

`pick_examples()` がこの 3 群(大きく勝った / 上記 2 種)を抜き出す。

### 順行(MFE)は、約定足で必ず水増しされる

**約定した足の高安をそのまま使ってはいけない。**売りは抵抗帯へ
下から登って約定するので、その足の安値は普通、登り始める前 =
**建玉を持つ前**に付いている。それを順行に数えると、持ってもいない
建玉の含み益を数えることになる。

不利側は削らない。**指値を越えて進んだ先がその足の不利側の端**
なのだから、順序を問う余地がない。ここまで削ると損切りを過小評価する。

実測(FX 10 銘柄 x 20 年 55,323 件)。同じ取引の「伸びたのに取れなかった」の割合:

| 約定足の順行 | 伸びたのに取れなかった |
| --- | ---: |
| 足の高安そのまま | 49% |
| **約定より後だけ** | **37%** |

この 12 ポイントぶんが水増し。**過去に「負けの 4〜5 割は順行して
から戻された」と書いたものは、すべてこの分だけ大きく出ている。**

## レンジを触らない(2026-08、18 銘柄で検証)

利用者が「一度も順行しなかった負け」の図を見て気づいた点:
**大半がレンジの中で、その上抜けを見てから入ってダマシに遭っている。**
裁量の鉄則「レンジは触らない、触るなら抜けたときではなく折り返しで」が
実装に入っていなかった。

### 定義は「向きが揃っているか」だけ

`entry.skip_range_structure` は `DowAnalyzer.structure_trend()` が
RANGE を返すとき(高値も安値も一方向に揃っていないとき)に見送る。
確定済みスイングしか見ないので先読みにならない。

**「帯がどれだけ水平か」では効かなかった。**直近 2 つの高値の差と
安値の差を ATR で割った「水平度」で四分位に切っても、最も水平な区分が
プラスになり、機構として説明が付かない。効いているのは向きの一致だけ。

同様に、値動きの直線性(効率比 = 終値の変化 / 1 本ごとの変化の合計)でも
分けてみたが、Q3 だけが突出する非単調なスパイクで、作法 1 を通らない。

### 損切りの追従は「どの足の押し安値か」で符号が変わる

利用者の指定(新しい高値ごとに手前の押し安値へ引き上げる)は正しかった。
以前「トレーリングは掛けるほど損」と結論したのは**初版仕様での測定**で、
基準足を分けていなかった。18 銘柄で測り直すと:

| 損切りの動かし方 | 取引 | 勝率 | 平均勝 | 最大勝 | 期待値 | t | プラス |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 動かさない | 690 | 17.7% | 4.95 R | 42.8 | +0.038 | +0.32 | 9/18 |
| H1 の押し安値へ | 696 | 32.8% | 1.02 R | 16.2 | -0.071 | -1.66 | 4/18 |
| H4 の押し安値へ | 696 | 25.7% | 2.08 R | 19.8 | -0.078 | -1.08 | 6/18 |
| **D1 の押し安値へ** | 696 | 18.7% | **4.71 R** | 42.8 | **+0.063** | +0.54 | 11/18 |

**エントリーと同じ足で追うと、この戦略がわざわざ座って待つはずの調整波
そのものに引っかかる。**押し安値が確定するのは値動きが一度戻った後だから。
方向を決めているのが D1 なら、損切りを預ける構造も D1 側に置く。

勝率は H1 追従が 32.8% で最も高い(負ける回数は半分近く減る)が、
平均勝ちが 4.95 R → 1.02 R、最大勝ちが 42.8 R → 16.2 R へ潰れる。
**減った負けより、削れた勝ち幅のほうが大きい。**

### 2 つを重ねた結果(FX 16 + CFD 2、開発用 2000-2019)

| 条件 | 取引 | 勝率 | 平均勝 | 期待値 | t | プラス銘柄 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 素の第 2 版 | 690 | 17.7% | 4.95 R | +0.038 | +0.32 | 9/18 |
| + レンジ除外 | 443 | 19.4% | 5.17 R | +0.182 | +1.29 | 10/18 |
| + D1 の押し安値へ追従 | 696 | 18.7% | 4.71 R | +0.063 | +0.54 | 11/18 |
| **+ 両方** | 445 | 20.0% | 5.06 R | **+0.214** | +1.52 | 12/18 |

### 作法 1〜4 の結果 — 3 勝 1 敗、負けた 1 つが本丸

| 作法 | 結果 | 中身 |
| --- | --- | --- |
| 1 近傍 | **通った** | 判定を弱→中→強で +0.063 → +0.183 → +0.669 と単調 |
| 2 期間 3 分割 | **通った** | +0.240 / +0.054 / +0.347。符号が反転しない |
| 3 信頼区間 | **通らない** | +0.214 R、95%CI -0.061 〜 +0.498。**0 をまたぐ** |
| 4 別銘柄 | 半分 | 18 銘柄中 12 がプラス(FX 16 中 11) |

作法 1 と 2 をこのプロジェクトで初めて通した構成。だが作法 3 が通らない
以上、**エッジがあるとは言えない。**1 銘柄 25 件しかなく、勝ちの分布に
太い右の裾があるため区間が広い。詰める方法は件数を増やすことだけ。

**そしてこの仮説は開発用データの図を見てから立てたもの。**同じデータで
確かめても確かめたことにならない。作法 3 が通ってから検証用へ 1 回だけ。

### CFD 2 銘柄はコストの土俵が違う

| 銘柄 | 取引 | コスト/R | 期待値 | コスト無し |
| --- | ---: | ---: | ---: | ---: |
| USD/JPY(FX の代表) | 32 | 0.01 | +0.524 | +0.534 |
| XAU/USD(金) | 19 | **0.21** | -0.146 | **+0.114** |
| SPX/USD(株価指数) | 8 | **0.35** | -1.186 | -1.000 |

FX 16 銘柄は往復コストがリスク幅の 1〜3% だが、金と株価指数は 21% と 35%。
H1 の値幅に対して CFD のスプレッドが広すぎる。**金はコストを外すと符号が
逆転する。**この 2 銘柄は「勝てない」のではなく「この時間足では払えない」。
FX 16 銘柄だけなら +0.220 R、プラス銘柄 11/16。

### まだやっていないこと

鉄則の後半「触るとしてもレンジを抜けた時ではなく折り返しで」は未実装。
帯の上限で売り、下限で買い、損切りは帯の外、利確は反対側の端。
逆張りモード(`entry.mode: fade`)の枠を流用すれば作れる。

**先に「触らない」を作法 3 まで通すこと。**両方同時に足すと、
どちらが効いたのか分からなくなる。なお過去の測定では逆張りの素のエッジ
(+0.03 R)はスプレッドに食われて ±0 止まりだった。

## 銘柄を 30 に増やして決着した(2026-08)— エッジは無い

レンジ除外 + D1 追従で 18 銘柄 +0.214 R(t=+1.52)まで来て、作法 1 と 2 を
初めて通した。そこで**相関の低いクロス 12 銘柄を足して 30 銘柄にした**ところ、
エッジは消えた。

| 母集団 | 取引 | 期待値 | t | プラス銘柄 |
| --- | ---: | ---: | ---: | ---: |
| 先に選んだ 16 銘柄(FX) | 418 | +0.257 R | +1.74 | 12/16 |
| **後から足した 12 銘柄** | 227 | **-0.406 R** | **-2.85** | **1/12** |
| FX 28 銘柄すべて | 645 | +0.024 R | +0.22 | 13/28 |

**後から足した銘柄は有意にマイナス。**プラス側の t=+1.74 より、
マイナス側の t=-2.85 のほうが強い。

### コストのせいではない

クロス通貨のスプレッドを 1.4〜3.0 pips と広めに置いたのが原因かを確かめた。

| 母集団 | コスト/R 中央 | 期待値 | コスト無し |
| --- | ---: | ---: | ---: |
| 先に選んだ 16 | 0.016 | +0.257 (t=+1.74) | +0.277 (t=+1.85) |
| 後から足した 12 | 0.033 | -0.406 (t=-2.85) | **-0.379 (t=-2.62)** |

コストが説明するのは -0.406 のうち 0.027 だけ。**ゼロにしても有意にマイナス。**
クロスは損切り幅も広いので、コストが R に占める比率はメジャーとほぼ同じ。

### 作法 2 も 28 銘柄では通らない

| 期間 | 件数 | 期待値 | t |
| --- | ---: | ---: | ---: |
| 2000-09 〜 2009-12 | 215 | +0.081 | +0.46 |
| 2010-01 〜 2015-02 | 215 | -0.002 | -0.01 |
| 2015-03 〜 2019-12 | 215 | -0.008 | -0.04 |

18 銘柄では 3 分割すべてプラス(+0.240 / +0.054 / +0.347)だった。
28 銘柄にすると 2010 年以降が丸ごと消える。

### 何が起きていたか

**あの 18 銘柄は、100 通り以上の条件探索にずっと使い回してきた銘柄だった。**
条件をその 18 銘柄に合わせて選び、同じ 18 銘柄で測っていた。
作法 4「別の銘柄でも成立するか」を、形だけ通して中身を通していなかった。

これで **7 回目**。毎回同じ形をしている:標本の中で条件を探す → 良い数字が出る
→ 標本を広げると消える。

### それでも再現したもの(28 銘柄でも形が保たれた)

| 事実 | 中身 |
| --- | --- |
| 追従先の序列 | H1 (-0.046) < H4 (-0.046) < なし (-0.001) < D1 (+0.024) |
| 固定の利確は勝ちの頭を押さえる | 外すと平均勝ちが 4.0 R → 7.6 R |
| 負けの 4 割は順行してから戻される | 入る場所ではなく持ち方で失っている |

序列は残るが、全体がゼロ付近なので勝てるわけではない。

### first_pullback_only は実データでは何も変えない

「レンジを抜けてトレンドが形成されたら**次の**調整波で入る」の
「次の」を字義どおり実装したが、8 通りすべてで数字が 1 桁も動かなかった。
配線ミスを疑って却下理由を数えたが、条件は正しく発火している
(EUR/USD で 5 回)。ただしその 5 回は下流でどのみち落ちるものだった。

**同時保有 1 建玉で保有が長いため、同じトレンドの中で 2 回目の押し目が
来る前に決着している。**つまりこの指定は既に実質的に満たされていた。

## 決済の型は、入り口の性質で効くかどうかが決まる(2026-08)

「負けの 34.6% は一度 +1.0R まで順行してから -1.0R で切られている」
という観測から、分割決済と建値移動を実装して測った。
**既存のダウ理論トレードでは、一切効かなかった。**

FX 28 銘柄・開発用 2000-2019・入り口は一切変えず決済の型だけを変えたもの:

| 決済の型 | 取引 | 勝率 | 平均勝 | 期待値 | t | 概算では |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| **いまのまま** | 642 | 17.9% | 4.79 R | **-0.001** | -0.01 | — |
| 0.5R で 50% + 建値へ | 644 | 65.4% | 0.47 R | -0.054 | -1.26 | +0.249 |
| 1.0R で 30% + 建値へ | 644 | 48.9% | 0.98 R | -0.057 | -0.88 | +0.236 |
| 1.0R で 50% + 建値へ | 644 | 49.2% | 0.98 R | -0.047 | -0.87 | +0.163 |
| 1.0R で 50%(建値なし) | 642 | 18.5% | 2.81 R | -0.010 | -0.15 | -0.010 |
| 2.0R で 50% + 建値へ | 643 | 33.0% | 2.10 R | +0.002 | +0.03 | +0.084 |
| 3.0R で 50% + 建値へ | 643 | 25.3% | 3.04 R | +0.003 | +0.03 | +0.050 |

### 利益が右の裾からしか来ていないなら、裾に触る操作はすべて損

642 件のうち **勝ち 115 件が合計 +551 R** を作っている。それ以外は全部
マイナス。**この戦略の収入源は裾だけ。**だから:

- 追従を掛ける → 裾を削る → 悪化
- 固定の利確 → 裾を切る → 悪化
- 分割決済 → 裾を薄める → 変わらない
- 建値移動 → 裾を最も強く削る → **最も悪化**

建値移動は勝率を 17.9% → 65.4% へ上げるが、平均勝ちを 4.79 R → 0.47 R へ
潰す。**中身が空になる。**

利用者の当初の要望「一つの勝ちの大きさを減らしても良いので、負ける数を
減らしたい」は、**この手法では原理的に不可能だった。**勝ちの大きさが
唯一の収入源だったため。

### 勝率は決済の刻み方だけで捏造できる

「0.5R で 70% 利確」は勝率 65.1% になるが期待値は -0.011。
70% を +0.5R で落とせば確定益 +0.35R、残り 30% が損切りでも -0.3R、
合計 +0.05R で「勝ち」に数えられるだけ。
**勝率という数字だけを見て手法を評価してはいけない。**

### MAE/MFE からの概算は、建値移動について強く楽観に出る

概算 +0.249 が実測 -0.054。差は 0.30 R で符号ごとひっくり返った。
概算は「一度 +L まで行った負けは建値で 0 になる」を数える一方、
**「一度 +L まで行って建値まで戻り、そこから走った勝ちも 0 で切られる」
を数えていない。**MAE/MFE には「戻ってから走ったか」の情報が無いため。

**建値移動を含む型は、必ず約定ロジックで回して確かめること。**

### 二階建ての位置づけの訂正

「決済の型は、どの根拠で入るかに関わらず必要になる層」と書いたのは
半分間違い。**入り口の性質によって効くかどうかが決まる。**

| 入り方 | 利益の出方 | 決済の型 |
| --- | --- | --- |
| 順張り | 稀な巨大な勝ち | 裾に触ると壊れる |
| 逆張り(抵抗帯) | 高い勝率・小さい勝ち | 効く余地がある。むしろ必須 |

逆張りは左の裾が太い(稀に全部持っていかれる)ので、資金管理なしでは
成立しない。**一階と二階は別々に決められない。セットで設計する。**

## 抵抗帯の手法(利用者が実際に使っていたもの)

利用者が全財産を注ぎ込んで作り、勝ち星は増えていたが資金管理で破産した
手法。**ダウ転換の順張りとはほぼ正反対。**

### なぜこの方向に見込みがあるか

こちらの測定がそのまま支持している。ダウ転換の上抜けを買うと、
**コストをゼロにしても -0.071 R(t=-5.33)。**その価格で発生した取引と
いう集団が、構造的に不利に選別されている。押し戻している側がいる。
ここまでで最も強い統計的事実がこれだった。

利用者の説明:「FX は大口投資家どうしの戦い。負け側の大口が抵抗帯で
資金を投入して押し戻そうとする。我々はその動きに乗る」。板も建玉も
見えないので説明そのものは確かめられないが、**確かめられるのは
「何度も試された水準は実際に止めている」という観測のほう。**

### 手順

1. **抵抗帯を見つける** — 何度も試され、そのたびに押し戻されている水準
2. **帯で待つ** — 抜けた瞬間に飛び乗らない
3. **帯が何を示したかに従う** — 弾かれたら跳ね返りに乗り、抜けたら
   抜けた側に乗る。**帯に来た時点では方向を決めない**
4. **伸ばす** — 跳ね返りで入った後、反対側の帯を抜けてそのまま走ったら
   その全部が取り分になる。**固定の利確を置くとこの裾を切り落とす**

### ダウ理論はここで「継続の確認」として使う(入り口ではない)

利用者の指摘がこの手法の要:

> 既にブレイクしたラインまでのレンジ分は取れている。ダウ理論が活きる
> 理由は、この後更に伸びるか伸びないかでしかない。エントリーの基準では
> ないからそもそも負けようがない。

**同じ道具が、入り口では毒で、出口では薬になる。**転換した瞬間に入ると
不利に選別されるが、既に含み益がある建玉を持ち続けるかの判断に使うなら、
外れても「伸びなかった」で終わる。`llmfx/domain/dow.py` はそのまま使える。

### 決済の優先順位(利用者と合意済み)

| 順 | 何を決めるか | 設定キー |
| --- | --- | --- |
| 1 | 損切りライン | `entry.pullback_stop_buffer_atr` ほか |
| 2 | 半分ずつ手仕舞いするライン | `execution.partial_exit_at_r` |
| 3 | 利確 — **明示しない**。相場に従う | `entry.use_take_profit: false` |

**利確のタイミングを決め打ちしない。**ダウ理論の検証で、利益が右の裾から
しか来ない手法に固定の利確を掛けると壊れることが分かっている
(平均勝ち 4.79 R -> 0.47 R)。この手法も裾から来るなら同じ壊れ方をする。

### 観測期間は「帯を引いた足」の尺度で決める

利用者の指摘:「15 分足にレンジが形成されていても、1 時間足では全く
異なる装いをしている可能性がある。見た足で使うレンジを決め、そこを
往復する」。**絶対時間ではなく、帯を引いた足の本数で数える。**

### まだ分かっていないこと

H1 で帯を引いて H1 で測ると、跳ね返りも突破もゼロだった
(最大到達 1.37 ATR / 最大逆行 1.39 ATR で完全に対称)。
M15 に落とすと非対称が出はじめた:

| 帯が示したもの | 件数 | 追随の平均 | 中央 |
| --- | ---: | ---: | ---: |
| 弾かれた | 429,823 | +0.011 | +0.052 |
| 中(帯の中で決まらない) | 121,760 | +0.014 | **+0.085** |
| 抜けた | 26,971 | **+0.039** | -0.114 |

「抜けた」は平均と中央値が逆を向いている。**突破の大半は失速するが、
少数が大きく走る。**利用者の言う「諦めた側の損切りに乗る」の形。

「中」は利用者の言う **帯での粘り合い**(小さなレンジ)。21% を占める。

**まだ R 倍数に直せていない。**帯の幅が中央値 2.48 ATR あり、損切りを
どこに置くかで結論が変わる。

## 抵抗帯の手法(第 3 版・図で確認済み。2026-08-22)

利用者が実際のチャート 3 枚(5 分足 / 15 分足 / 1 時間足)に線を引いて
見せてくれたことで、**こちらが「抵抗帯」と呼んでいたものが別物だった**
と分かった。以下が図で確認の取れた正。

| 項目 | 決定 | 設定 |
| --- | --- | --- |
| 帯の正体 | **映っている範囲の端**。スイングの塊ではない | `zone_source: range` |
| 端の条件 | その最値で **折り返していること**。いまの動きの端は使わない | `range_needs_turn: True` |
| 窓 | **1 週間**。FX は週 120 時間なので H1 で 120 本 | `range_bars: 120` |
| 帯を引く足 | エントリーの 2 つ上(M15 なら H1) | `higher_minutes: 60` |
| エントリー | 端そのもの(少し奥に置くなら `entry_beyond_atr`) | `entry_at_zone_extreme: True` |
| 損切りの物差し | **下位足**(M15)。遠くならないように | `scale_to_zone_timeframe: False` |
| 損切りの幅 | **未確定。利用者から「検証するタイミング」と指定あり** | `stop_buffer_atr` |
| 利確 | 反対側の端 | `exit_at_opposite_zone: True` |

利用者の言葉:

> 15 分足でエントリーするなら、1 時間足を見て抵抗帯を見つけ、その最値で
> 15 分足の基準でエントリーする。ここで下位足の基準でエントリーするのは、
> 損切ラインが遠くならない為と、利確ラインへの伸びが大きく期待出来るからだ。

> 何年も前の抵抗帯に意味はない。彼らはもうそこにはいない。

> 約定にかからないのはそれはそれで仕方がない。エントリーしなければ
> そもそも負けない。

### 何が違っていたか

初版〜第 2 版は「確定スイングを ATR 0.5 でまとめた、2 回以上試された水準」
を帯としていた。これは **範囲の内側** に出る。利用者が線を引くのは
**範囲の外枠**。内側の水準で逆張りすれば当然どちらへも抜けられる。

利用者が図を見て言ったこと(`docs/zone-trades.html` の 4 例):

- 1 枚目「安値切り上げが始まっているので避けた方が良さそう」
  → `skip_break_risk` がまさにその判定。4 銘柄すべてで改善した
- 2 枚目「そもそも抵抗帯にはあまり見えない」
- 4 枚目「何を見て抵抗帯と判断したのかがわからない」

**接触回数を増やす方向は逆効果だった**(4 回以上・6 回以上と絞るほど
単調に悪化)。「何度も試された水準ほど強い」はこの作り方では成り立たない。

## 1 本の足の中の道順を仮定してはいけない(最重要・2026-08-22)

四本値からは **1 本の足の中で高値と安値のどちらが先だったか分からない**。
逆張りで最値に指値を置く手法では、ここの仮定が結論を丸ごと決める。

売りの指値は直近の高値に置く。その高値に届く足は **下から登ってきた足**
なので、その足の安値は登り始める前 = **約定より前**に付いていることが多い。
高安だけを見ていると、建玉を持つ前に通り過ぎた値動きを取り分に数える。

USD/JPY 開発用(M1 322 万本で道順を解決):

| 扱い | 差引 | 勝率 |
| --- | ---: | ---: |
| 高安だけ・利確は約定足も認める(旧既定) | **+0.126** | 61.3% |
| 陽線 始値→安値→高値→終値 と推し量る | -0.130 | 48.2% |
| **M1 で順序そのものを解く** | **-0.058** | 53.0% |
| 約定足での利確を全部外す | -0.184 | 44.6% |

**符号が変わる。**旧既定は損切りだけ不利側・利確は有利側という
食い違いがあった。以後この一族の検証は必ず `intrabar="path"` で行う。

    ts = collect_fade_trades(m15, ..., intrabar="path", path_candles=m1)

M1 は `llmfx data fetch-histdata --granularity M1` で取る。
1 銘柄 9 年で 320 万本・200MB。**取れた年ごとにファイルへ吐く**ので
メモリは尽きない。

### 凍結した仮説(`docs/frozen-hypothesis.md`)の数字は全部無効

第 2 版として記録した「608,444 件 差引 +0.084 R / R月 +9.88 /
四つの確認すべて通過」は、次の 2 つの上に載っていた:

1. 反対側の帯での決済が損切りを飛び越えるバグ(平均負け -2.47 R、最悪 -35 R)
2. 上記の道順の仮定

直して測り直すと **素の期待値がほぼゼロ**(-0.019 〜 +0.013、4 銘柄)。
「エッジはあるがコストに食われる」ではなく、**素で何も無い**。

## 帯を抜けた側で取る(第 2 号の仮説)— 作法 4 で落ちた。8 回目

利用者の説明「弾かれたら跳ね返りに乗り、**抜けたら抜けた側に乗る**」の
後半を実装した。同じ帯・同じ待ち方で、向きだけを逆にする。

跳ね返り側は 4 銘柄すべてマイナス、抜けた側は 3 銘柄でプラスと
**符号が反転**した。条件探しに使った 4 銘柄で作法 1・2・3 を通した
(このプロジェクトで初めて 3 つ同時に通った)。条件と予想を
`docs/frozen-break.md` に凍結してから、未使用 14 銘柄へ掛けた。

| 母集団 | 銘柄 | 取引 | 素 | 差引 | t | プラス |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 条件探しに使った 4 | 4 | 4,806 | +0.164 | +0.117 | +2.37 | 3/4 |
| **使っていない 14** | 14 | 17,528 | **+0.005** | **-0.088** | **-3.72** | **3/14** |

**素の時点でゼロ。**コストのせいではない。狭いスプレッドの 7 銘柄だけに
絞っても -0.008(t=-0.22)。期間 3 分割もすべてマイナス。

### 学び — 作法 1〜3 は独立性を担保しない

**作法 1・2・3 は、条件を探した標本の中でしか回っていない。**同じ 4 銘柄で
近傍を見ても、期間を割っても、ブートストラップを回しても、
その 4 銘柄に共通する偶然はそのまま残る。**独立性を担保するのは作法 4 だけ。**

今回は「初めて 3 つ同時に通った」ことで期待を上げてしまった。
**通った数ではなく、どれが通ったかを見ること。**

### 残っていた 2 つも埋めた(2026-08-22)— どちらもゼロ

利用者の手法のうち未実装だった 2 つを入れた。**条件探しに使った 4 銘柄
だけで測っている**(残り 14 銘柄は確認用に温存)。

**1. 押し負けが起きた時点で乗る**(`edge_mode="weakening"`)

帯へ届くのを待たず、抵抗帯へ向かう安値が切り上がった瞬間に次の足の
始値で乗る。損切りはその切り上がった安値の下。

| 損切り | 保有 | USD/JPY | EUR/USD | GBP/JPY | AUD/USD | 素の平均 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 0.75 | 1 日 | -0.002 | -0.029 | -0.022 | -0.079 | +0.003 |
| 1.5 | 1 日 | -0.056 | -0.008 | +0.015 | -0.058 | -0.001 |
| 2.5 | 1 日 | -0.037 | +0.001 | -0.012 | -0.051 | -0.006 |
| 1.5 | 2.5 日 | -0.102 | +0.011 | -0.008 | -0.087 | -0.022 |

**素の平均がゼロ。**どこを取っても動かない。

ただし**形は変わる**。勝率 32〜40%、平均勝ち 1.2〜2.9 R。抜けた側
(勝率 15%、平均勝ち 7 R)とは別物で、利用者が当初望んだ
「勝ちを小さくして負ける回数を減らす」形に近い。**期待値がゼロなだけ。**

**2. 同時保有を増やす**(`max_open`)

利用者の指摘「レンジの往復を取る時に建玉があると指値を入れられない
= 機会損失」。1 → 4 にすると取引数は 2.2 倍になるが、
**1 取引あたりの期待値は下がる**:

| 銘柄 | 同時 1 | 同時 4 | R/月(1) | R/月(4) |
| --- | ---: | ---: | ---: | ---: |
| USD/JPY | +0.236 | +0.171 | +2.59 | +4.25 |
| GBP/JPY | +0.209 | +0.169 | +2.36 | +4.17 |
| EUR/USD | +0.072 | -0.006 | +0.80 | -0.15 |
| AUD/USD | -0.048 | -0.160 | -0.54 | -4.25 |

**後から入る建玉のほうが悪い。**枠が空くのを待っていた取引は、
待たされた時点で条件が落ちている。プラスの銘柄では R/月 が増えるが、
**マイナスの銘柄では傷が広がる。**符号を変える力は無い。

### それでも残った事実

| 事実 | 中身 |
| --- | --- |
| 抜けた側 > 跳ね返り側 | 18 銘柄でも素 +0.040 対 大幅マイナス。序列は保たれる |
| 抜けを終値で確認すると悪化 | 4 銘柄中 3 つ。待つ間に動いた幅がそのまま取り分から消える |
| 押し負け(安値切り上げ)は選別にならない | 4 銘柄で 2 対 2 に割れる |
| クロス通貨はコストで沈む | コスト 0.08 以上の 7 銘柄は 0/7 |

## 建値への損切り移動 — 9 回目。ただし初めて標本を広げる前に見つけた

「負けの半分は一度こちらへ伸びてから戻される」という観測から、
建値へ損切りを動かす手を測った。**最初に出た数字は丸ごと作り物だった。**

FX 10 銘柄 x 20 年・開発用 2000-2019・55,323 件。入り口は第 3 版の
抵抗帯(H1 で 1 週間の端、M15 で指値、損切り 1.5 ATR、同時 4 建玉)。

| 建値へ | 件数 | 素(汚れ) | 素(直後) | 差引(直後) | 伸びて取れず | プラス銘柄 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| なし | 55,323 | -0.042 | -0.042 | -0.070 | 37% | 0/10 |
| 0.05 R | 56,379 | — | **-0.003** | **-0.031** | **21%** | 1/10 |
| 0.1 R | 56,379 | — | -0.003 | -0.031 | 23% | 0/10 |
| **0.2 R** | 56,379 | **+0.064** | -0.011 | -0.038 | 25% | **0/10** |
| 0.5 R | 56,375 | +0.041 | -0.025 | -0.052 | 31% | 0/10 |
| 1.0 R | 56,315 | -0.002 | -0.031 | -0.058 | 43% | 0/10 |

「素(汚れ)」の列では **10 銘柄すべてがプラス**になり、条件探しに
使った 4 銘柄(+0.036)と使っていない 6 銘柄(+0.037)まで一致していた。
**このプロジェクトで初めて作法 4 まで通って見えた数字**が、
約定足の順行の水増し(前述)だけで出来ていた。

### 気づけた理由は、良い数字そのものではなく形

引っかかったのは 2 点。**最良値がグリッドの端(最小の 0.2)にあった**こと、
**勝率が 2.1% まで落ちていた**こと。閾値を小さくするほど良くなるなら、
それは「機構が効いている」ではなく「条件が緩いほど良い」であって、
説明になっていない。調べたら緩いほど約定前の値動きで発火していた。

**8 回は全部、銘柄を足してから消えた。今回が初めて事前に見つけた分。**

### 直した後に残るもの

建値移動そのものは本物の改善(-0.070 → -0.031、0.039 R)。だが:

- **0/10 銘柄。**どれも黒字にならない
- **最良が閾値の最小端**(0.05 R でほぼ即座に建値へ逃がす)。
  これは機構ではなく、**この入り口で建玉を持ち続けること自体が損**
- 勝率 12.0% → 2.8%、平均勝ちは +6.98 → +8.02 とむしろ伸びる。
  裾は削っていない。削っているのは「戻ってくる負け」だけ

道順の仮定 3 通り(`stop_first` / `ohlc` / `no_same_bar_profit`)で
挟んだ結果、直した 2 つは 0.001 差で一致した。**M1 で解いても救われない。**

## 弾いた足で入る(第 3 号)— 10 回目で初めて作法 4 を通った

利用者の説明を実装した:

> 抵抗帯に触れた時のローソク足の跳ね上がりを見たらもうエントリー
> していることもあった。体感的にはその時の方がまだ + を取れていた。

条件は `docs/frozen-rejection.md` に凍結してある。**数字を見る前に
「通らない」と予想を書いて外した。**

| 母集団 | 銘柄 | 件数 | 差引 | t | プラス |
| --- | ---: | ---: | ---: | ---: | ---: |
| 条件探しに使った 4 | 4 | 16,844 | +0.044 | +4.53 | 4/4 |
| **使っていない 6** | 6 | 24,826 | **+0.022** | **+2.99** | **4/6** |
| 10 銘柄すべて | 10 | 41,670 | +0.031 | +5.24 | 8/10 |

**素(コスト無し)は 10/10 銘柄がプラス。**このプロジェクトで初めて。
先読みの確認も済み(実データを 50/70/90% で打ち切って食い違い 0 件)。

### 利用者の仮説は半分外れた。外れたほうが本人の元の言葉と合う

| 仮説 | 判定 |
| --- | --- |
| 触れられるほど脆くなる → **早いほど有益** | **否定。**同じ帯のまま到達回数で割ると 2 回目 -0.107 / 3 回目 +0.028 / 4 回目 -0.096 |
| 検知するのは **押し戻しの強さ** | **否定。**ヒゲを 0.0 → 1.0 ATR と要求するほど単調に悪化 |
| 跳ね上がりを見て **もう入る** | **これが当たり** |

効いているのは強さではなく **タイミング**。ヒゲの大きさを問わず
「帯に触れて、その足が帯の内側へ戻って引けた」ことだけを確認して
次の足で入るのが最良。**待つと待った分だけ取り分が減る。**

### コストは「差し引き」ではなく「発動位置のずれ」で入れる

利用者の確認から直した。足は Bid なので:

| | 買い | 売り |
| --- | --- | --- |
| 入る | Ask を払う。Bid が **スプレッドぶん余計に** 動かないと届かない | Bid で売る。そのまま |
| 損切り | Bid で発動 | Ask で発動。**スプレッドぶん手前で刺さる** |

平均では定額を引くのと近いが、**建値へ動かす閾値がスプレッドと
同じ桁のときは結果が変わる。**この手法はまさにそこに乗っている。
直した結果、未使用 6 銘柄が +0.018 → **+0.022**(t=+2.33 → +2.99)。

### 銘柄は「pips の狭さ」ではなく **コスト/R** で選ぶ

| コスト/R の上限 | 銘柄 | 実勢 | x1.5 | x2 | x3 |
| ---: | ---: | ---: | ---: | ---: | ---: |
| **0.025** | **3** | **+0.051** | **+0.040** | **+0.029** | **+0.012** |
| 0.030 | 6 | +0.045 | +0.030 | +0.015 | -0.011 |
| 0.040 | 9 | +0.035 | +0.015 | -0.003 | -0.031 |
| 上限なし | 10 | +0.031 | +0.008 | -0.011 | -0.042 |

コスト/R と結果の順位相関は **-0.66(実勢)〜 -0.90(x3)**。締めるほど
単調に良くなる。**結果を見てから銘柄を選ぶと 9 回崩れたのと同じ形に
なるが、コスト/R は 1 件も取引する前に計算できる**ので選択バイアスに
ならない。

0.025 で切ると **USD/JPY(0.011)・EUR/USD(0.015)・GBP/USD(0.023)**。
GBP/USD は条件探しに使っていない銘柄。スプレッド 2 倍でも 3/3 が残る。
合計 **R/月 +2.71**。

GBP/JPY はスプレッドが最も広い 0.90p だが値幅も大きく、コスト/R は
0.027 で 6 番目。**AUD/USD は 0.60p でもコスト/R 0.039** で実勢の
1.5 倍で沈む。**pips で選ぶと間違える。**

### 残っている弱点

1. **建値移動が無いと成立しない**(-0.066、0/10)。閾値の軸に山が無く、
   0 へ向かって単調に上がる。止まるのはスプレッドという物理の端だけ
2. **窓が狭い。**閾値を 0.05 R から 0.1 R へ倍にするだけで、未使用 6 が
   +0.022 → +0.001 になる。0.05 R は 0.75〜1.66 pips
3. **未使用 6 銘柄はスプレッド 1.5 倍で -0.005。**実勢でしか成立しない。
   コスト/R で絞った 3 銘柄だけが 2 倍・3 倍に耐える
4. R/月 +2.71(3 銘柄合計)。**目標の月利 1.4 倍からは桁が違う**
5. **検証用データ(2020 年以降)はまだ使っていない**

## 報告の仕方 — 手法ごとに分けて出す(利用者の指摘、2026-08-23)

> 一つにまとめられて ± を出されると、どの手法が効いているのか、
> 或いはどの手法が足を引っ張っていて改善しなければならないのかがわからない。

**合算の 1 行だけを出してはいけない。**過去に「同時に複数動かすと、
どれが効いたかわからなくなる」という指摘を受けているのに、
**報告の側で同じことをしていた。**

建玉ごとに **どの機構で生まれたか** を記録し、そこで割って出す:

| 分けるもの | これが分かる |
| --- | --- |
| 最初のエントリー(帯の端の指値) | 入り口そのものに優位性があるか |
| ドテン(転換ラインの注文) | 取り分を作っているか、傷を広げているか |
| 買い増し | 後から足した建玉が最初より良いか悪いか |
| 転換での手仕舞い | 出口が勝ちを伸ばしているか、切っているか |
| 損切りの移動(1 つ前へ) | 追従が守っているか、削っているか |

それぞれ **件数・勝率・平均勝ち・期待値 R・t 値・R/月・プラスの銘柄数**。
合算は最後に 1 行だけ添える。

## 第4版(ダウ転換で手仕舞ってドテン)の初回測定 — 明確にマイナス

`llmfx/research/zone_swing.py`。構造は H1、執行は M15、成行を使わず、
出口は「ダウ転換」と「損切り」の 2 つだけ。損切りは折り返すたび
**1 つ前**の高値・安値へ。条件探しに使う 4 銘柄・開発用 2000-2019。

| 機構 | 件数 | 勝率 | 平均勝 | 期待値 R | t | プラス |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| **zone**(帯の指値) | 23,399 | 17.5% | +2.05 | **-0.217** | -19.2 | 0/4 |
| **flip**(ドテン) | 7,218 | 30.3% | +1.47 | -0.070 | -4.2 | 0/4 |
| **add**(買い増し) | 7,000 台 | — | — | **-0.014** | -1.1 | **2/4** |
| 合算 | 30,617 | 20.5% | +1.85 | -0.182 | -19.2 | 0/4 |

### 機構ごとに割って初めて見えたこと

**足を引っ張っているのは帯の指値そのもの。**続きに乗る側(ドテン・
買い増し)はほぼゼロで、買い増しは 4 銘柄中 2 つがプラス。

ドテンや買い増しを増やすと合算は良くなる(-0.182 → -0.138)が、
**zone の -0.217 は 1 つも動かない。**マシな脚を混ぜて薄めているだけ。

**これは「抜けた側 > 跳ね返り側」の再確認**でもある。第3版でも
弾いたのを確かめてから乗る形(素 +0.071、10/10)のほうが、帯で待つ
指値より良かった。**確認の価値が、良い価格より大きい。**

### 掃引はすべて平ら

| 軸 | 動かした範囲 | 合算 R |
| --- | --- | --- |
| 指値の ± | -0.3 〜 +3.0 ATR | -0.19 〜 -0.17。**平ら** |
| 折り返しへ預ける余裕 | 0 〜 1.0 ATR | -0.18 〜 -0.17 |
| 転換の合図 | 両方 / 片方 | -0.182 / -0.179。差なし |
| ドテンの回数 | 0 〜 3 | -0.212 〜 -0.169(薄めているだけ) |
| 買い増しの回数 | 0 〜 3 | -0.182 〜 -0.138(同上) |
| **同時保有** | 1 〜 16 | **-0.187 〜 -0.180。効かない** |
| 最初の損切り | 1.0 〜 7.0 ATR | -0.271 〜 -0.053(下記) |

**同時保有は効かない。**時間切れを外すと枠が埋まって機会損失に
なるはず、という懸念は数字に出なかった(1 でも 16 でも -0.18)。

**最初の損切りを広げると良く見えるのは薄まっているだけ。**
1.0 → 7.0 ATR で平均勝ちが +1.33 → +0.83、平均負けが -0.62 → -0.47。
R の物差しが伸びて全部が縮んでいる。R/月 は -13.98 → -6.94 と本当に
減るが、符号は変わらない。

### この日に見つけたバグ 2 つ — どちらも派手な嘘を出していた

| バグ | 出ていた嘘 | 中身 |
| --- | --- | --- |
| 転換ラインを「抜けた」と判定していなかった | **+0.326 R, t=+31.8** | 帯の上端で売った瞬間、直近の確定高値は既に価格より下。そこへ「届いた」ことにすると建てた足でいきなり利確する |
| 待ち伏せの解除を **帯** からの距離で測っていた | **+0.946 R, t=+14.2** | 注文は帯 + オフセットに置いてあるのに、近接判定は帯基準。奥へ置くほど「大きくヒゲを出して帯の近くへ戻った足」だけが選別される。狙って作った選別ではない |

**どちらも報告する前に潰せた。**引っかかったのは数字の大きさではなく
**形**(グリッドの端が最良・単調すぎる・勝率が異常)。

## 現在地と次にやること

**結論: ダウ理論の転換を根拠にした売買に、検証できる範囲でエッジは無い。**
数字を良く見せないという方針どおり、そのまま残す。

試して否定されたもの(すべて開発用 2000-2019、FX 最大 28 銘柄):

| 方向 | 結果 |
| --- | --- |
| ブレイク買い(初版仕様) | 22,264 件 -0.114 R t=-7.68 コスト無しでも -0.071 |
| 逆張り(fade) | 素のエッジ +0.033 R はスプレッドに食われる |
| 上位足フィルタ | 全変種が有意にマイナス。8 銘柄中 0〜1 しかプラスにならない |
| 時間帯フィルタ | 小標本で +0.276 → 大標本で否定 |
| 押し目待ち(第 2 版仕様) | 16 銘柄 +0.257 → 新規 12 銘柄 -0.406(t=-2.85) |
| レンジ除外 + D1 追従 | 同上。銘柄を広げると消える |

出来上がっているもの:
- ダウ転換の検出、バックテスト(先読み防止つき)、ペーパー取引、LLM 層
- HistData / GMOコイン / Dukascopy / MT4 からのデータ取得
- 30 銘柄 x 20 年を 8 通り並列で回し、作法 1〜4 に掛ける仕組み
- 上位足・時間帯・指標予定・ボラティリティ・レンジの各フィルタ
- トレードの切り出しと逆行/順行の集計(`llmfx/backtest/inspect.py`)

**検証用データ(2020 年以降)は一度も触っていない。**6 年半ぶん手つかず。
次の仮説が作法 1〜4 を通ってから、1 回だけ使う。

次に取れる道(利用者と相談中):
1. **ダウ転換から離れる。**転換の検出は前提であって根拠ではないと割り切る
2. **値動き以外の入力を入れる。**指標の実績と予想の乖離、金利差、建玉統計。
   値動きだけで 100 通り以上試して駄目だったので、情報を増やすしかない。
   ただし取得の手間が跳ね上がり、そこにエッジがある保証も無い
3. **手を止める。**ここまでで出た答えは「無い」。これも成果ではある

未検証・既知の制約:
- **OANDA への実接続は未テスト**。初回はデモ口座かつ最小ロットで
- 同時保有は 1 銘柄・1 建玉。複数通貨ペアへの分散は未実装
- FX のスワップポイントは未計上
- CFD(金・株価指数)は H1 ではコストが R の 2〜3 割。土俵が違う
