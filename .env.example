# ---- Claude (LLM layer) ----
# 未設定でもアプリは動作します(LLM層が自動的に無効化されるだけ)
ANTHROPIC_API_KEY=

# ---- OANDA v20 (データ取得 / ペーパー取引) ----
# デモ口座: https://api-fxpractice.oanda.com
# 本番口座: https://api-fxtrade.oanda.com  ※本番は現時点で未サポート(安全のため)
OANDA_API_TOKEN=
OANDA_ACCOUNT_ID=
OANDA_ENV=practice
