# 突き合わせ用の伏せ札

`blank-scenes-hidden.json` は `docs/blank-scenes.html`(素のチャート 6 場面)に
対応する **伏せてある答え**。

- `future_high` / `future_low` / `future_close` — その先 240 本(2.5 日)の値動き
- `ours` — こちらの規則が同じあたりで引いた線と、その結果

**利用者の線をもらう前に見せない。**先に見せると、こちらの数字に寄せた線に
なってしまい、突き合わせの意味が消える。
